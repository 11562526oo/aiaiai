跳表，其实就是多层链表。底层链表包含所有数据，上层是下层的子集，从O(n)优化到O(logn)
redis: 增加回退指针，score能重复


创建跳表：
	首节点（Sentinel）底层链表包含所有数据，上层是下层的子集
	以随机方式向上层添加数据
	推荐是logn层，保证查找和插入数据都是logn

查找跳表
	从头查，如果小于下一个节点的分数，或者下一个节点是null下沉一层

插入跳表
	先找到要插入的位置。
	创建一个表，放入每一层的位置，包含头部的Sentinel（用于插入位置确定）
	创建一个要插入的节点
	仍然以随机的方式是否增加高度
